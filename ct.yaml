remote: origin
chart-dirs:
  - helm
namespace: default
release-label: default
# The auto-generated names don't work for ct install
# We will have to figure out a way to make sure generated
# names don't have hyphen, otherwise we cannot run ct install
# helm-extra-args: --name-template='test-{{randAlpha 4 | lower}}'

# Bug: ct.yaml should be within helm folder but it generates an error
# when validating for charts repo - seems related to absolute path -
