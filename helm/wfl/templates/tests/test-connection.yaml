apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wfl.fullname" . }}-test-connection"
  labels:
    {{- include "wfl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wfl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
